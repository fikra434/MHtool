/* ============================== typing animation ============================ */
var typed = new Typed(".typing",{
    strings:[""," Validate XML file with DTD"," Validate XML file with XSD"," Convert XML to JSON"," Convert DTD to XSD"],
    typeSpeed:100,
    BackSpeed:60,
    loop:true
})